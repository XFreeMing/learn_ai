# 依赖现由 uv 管理（见 pyproject.toml / uv.lock）。
# 本文件仅作非 uv 用户的参考，运行 `uv sync` 即可安装下列全部依赖。

# M1 + M2 基础依赖（CPU 即可）
numpy>=1.24
torch>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# M3 baseline 对照组
transformers>=4.40

# M3 推理引擎（仅 Linux/WSL + NVIDIA GPU，单独安装）
# vllm>=0.5
